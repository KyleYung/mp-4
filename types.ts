export type ArtProps = {
    id: number;
    title: string;
    image_url: string;
    dated: string;
};

export type HarvardArtItem = {
    id: number;
    title: string;
    primaryimageurl: string;
    dated: string;
  };